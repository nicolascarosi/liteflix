const BASE_URL = 'https://api-liteflix.vercel.app/api'

export const endpoints = {
  UPLOAD: `${BASE_URL}/images/upload`,
  NOW_PLAYING: `${BASE_URL}/movies/now_playing`,
  POPULAR: `${BASE_URL}/movies/popular`,
}
